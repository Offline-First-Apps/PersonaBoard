# URL of the backend server (used at build time — prefix MUST be NEXT_PUBLIC_)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Public URL of the Windows installer (R2 S3 endpoint)
NEXT_PUBLIC_DOWNLOAD_URL=https://7b71f489541fe72763c158b881ed7ccb.r2.cloudflarestorage.com/backgroundgone/PersonaBoard-Setup.exe

# Set to "1" to skip t3-env validation at build time
# SKIP_ENV_VALIDATION=1
